

import * as sh from "lib/shorthands/schema/manual"


// import { $ as x_json } from "./schemas/json/$.module.astn.js"

export const $ = sh.st.set({
    //empty for now, but needed for the generic code
    // "json": x_json,
})